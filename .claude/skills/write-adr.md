# Skill: Write ADR

Use when recording an Architecture Decision Record.

## Steps

1. Create `docs/decisions/ADR-XXX-slug.md` using the template in `docs/decisions/README.md`.
2. Include Context, Decision, Alternatives, Trade-offs, Consequences.
3. Link related docs if needed.
4. Do not rewrite old ADRs; supersede with a new number.
