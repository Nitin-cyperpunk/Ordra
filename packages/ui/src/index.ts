/**
 * Shared UI package.
 * Components will be added via shadcn/ui as features need them.
 * Prefer apps/web/components/ui during early development; promote reusable
 * primitives here when used across apps.
 */

export {};
